<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aicsp.user.mapper.UserRoleMapper">
    <update id="deleteByUserId">
        UPDATE cs_user_role SET deleted = TRUE, updated_at = CURRENT_TIMESTAMP WHERE user_id = #{userId} AND deleted = FALSE
    </update>
    <insert id="insert">
        INSERT INTO cs_user_role(id,user_id,role_id,created_by,updated_by) VALUES(#{id},#{userId},#{roleId},0,0)
    </insert>
    <select id="selectRoleIdsByUserId" resultType="long">
        SELECT role_id FROM cs_user_role WHERE user_id = #{userId} AND deleted = FALSE ORDER BY role_id
    </select>
</mapper>
