spring:
  profiles:
    active: @spring.profiles.active@
  application:
    name: stream-service
logging:
  config: classpath:logback-spring.xml
  file:
    path: stream-service/logs
management:
  endpoints:
    web:
      exposure:
        include: health,info
